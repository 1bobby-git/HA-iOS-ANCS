# 변경 이력

## Home Assistant 자동화 예시 - 2026-09-02

- 배달의민족 iOS 앱(`com.jawebs.baedal`, `배달의민족`)의 `곧 도착합니다!` 및 `문앞으로 배달이 완료되었습니다.` 알림만 식별해 스피커로 안내하는 TTS 예시를 추가했습니다.
- 앱 ID와 앱 이름을 모두 정확히 검사하고, 완전한 신규 알림만 처리해 쿠폰·광고 등 다른 알림은 방송하지 않습니다.

## 펌웨어 0.3.8 - 2026-09-02

- iPhone Bluetooth 페어링 PIN을 모든 지원 보드에서 `123456`으로 고정했습니다.
- 등록 창이 닫힌 상태에서는 신규 페어링을 허용하지 않는 기존 120초 제한을 유지합니다.
- 설정 포털과 설치 문서에서 고정 PIN을 명확하게 표시합니다.
- 특정 아파트 앱의 `app_id`와 `app_name`이 모두 정확히 일치할 때만 차량 입차 문구를 추출하여 TTS로 전송하는 Home Assistant 자동화 예시를 추가했습니다.
- ESP32 7개 지원 대상의 0.3.8 factory 이미지와 SHA256 지문을 갱신했습니다.

## 펌웨어 0.3.7 - 2026-09-02

- 16자 NVS 네임스페이스 때문에 ANCS 초기화가 중단되던 문제를 수정해 enroll 등록 창이 정상 동작하도록 했습니다.
- 설정 포털에 `iPhone 기기 등록` 버튼과 6자리 등록 코드 표시를 추가했습니다.
- 연결된 Wi-Fi가 순간적으로 끊겨도 45초 동안 자동 재연결한 뒤에만 복구 AP로 전환합니다.
- 동일 SSID 다중 AP에서 신호 강도 우선 선택과 드라이버 재시도를 적용하고 설정 AP 동시 접속 허용 수를 2대로 늘렸습니다.
- HTTP 서버 스택을 8KB로 늘려 설정 페이지 처리 중 스택 보호 재부팅을 방지합니다.

## Home Assistant 통합 0.6.8 / 펌웨어 0.3.6 - 2026-09-01

- 알림 전송 제외 누계를 MQTT 미연결·정책 필터·내부 처리 실패로 분리했습니다.
- 단일 합계를 ‘연결 장애’로 표시하던 문구를 부팅 후 누적 사유별 표시로 교체했습니다.
- 현재 MQTT 연결 여부와 과거 제외 누계를 분리해 정상 연결 후에도 원인을 오해하지 않도록 했습니다.
- 이전 펌웨어의 합계만 수신할 때는 원인 구분 미지원임을 명시합니다.

## 펌웨어 0.3.5 - 2026-09-01

- 설정 포털에서 BLE 연결 없이 ESP32 → MQTT → Home Assistant 테스트 알림을 전송할 수 있습니다.
- MQTT 연결 직후 현재 Discovery를 우선 발행하고, 엔티티별 4초 대기를 제거해 최초 기기 등록 시간을 수분에서 수초 수준으로 줄였습니다.
- MQTT 상태를 2초마다 자동 갱신하고 TCP 시간 초과·포트 거부·인증 거부·재시도 간격을 표시합니다.
- MQTT 재연결을 5초 → 15초 → 30초 → 60초로 제한하고 중복 연결 시도를 차단했습니다.
- 반복 BLE 인증 실패 3회 시 무한 재연결을 중지하고 iPhone 등록 복구 절차를 안내합니다.
- 고정 PIN을 장치별 무작위 6자리 코드로 교체하고 설정 AP를 1명 접속 및 선택적 PMF로 강화했습니다.
- HTTP LRU 정리와 소켓 제한을 적용해 복구 포털의 파일 디스크립터 고갈을 방지합니다.
